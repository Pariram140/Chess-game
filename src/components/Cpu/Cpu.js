// CPU.js
class CPU {
    static generateMove() {
      // Generate a random move for the CPU
      // Implement CPU move logic
      // For now, let's generate a random move
      const move = {
        from: 'e2',
        to: 'e4'
      };
      return move;
    }
  }
  
  export default CPU;
  